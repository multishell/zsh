name=mapfile

moddeps="zsh/mapfile"

objects="mapfile.o"

:<<\Make
mapfile.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

mapfile.c: alias.c.in
	sed 's,@NAME@,mapfile,g' < $(sdir)/alias.c.in > $@

clean-here: clean.mapfile
clean.mapfile:
	rm -f mapfile.c
Make
