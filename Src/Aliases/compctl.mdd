name=compctl

moddeps="zsh/compctl"

objects="compctl.o"

:<<\Make
compctl.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

compctl.c: alias.c.in
	sed 's,@NAME@,compctl,g' < $(sdir)/alias.c.in > $@

clean-here: clean.compctl
clean.compctl:
	rm -f compctl.c
Make
