name=sched

moddeps="zsh/sched"

objects="sched.o"

:<<\Make
sched.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

sched.c: alias.c.in
	sed 's,@NAME@,sched,g' < $(sdir)/alias.c.in > $@

clean-here: clean.sched
clean.sched:
	rm -f sched.c
Make
