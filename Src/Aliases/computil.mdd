name=computil

moddeps="zsh/computil"

objects="computil.o"

:<<\Make
computil.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

computil.c: alias.c.in
	sed 's,@NAME@,computil,g' < $(sdir)/alias.c.in > $@

clean-here: clean.computil
clean.computil:
	rm -f computil.c
Make
