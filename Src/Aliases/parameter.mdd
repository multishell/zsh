name=parameter

moddeps="zsh/parameter"

objects="parameter.o"

:<<\Make
parameter.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

parameter.c: alias.c.in
	sed 's,@NAME@,parameter,g' < $(sdir)/alias.c.in > $@

clean-here: clean.parameter
clean.parameter:
	rm -f parameter.c
Make
