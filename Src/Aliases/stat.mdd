name=stat

moddeps="zsh/stat"

objects="stat.o"

:<<\Make
stat.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

stat.c: alias.c.in
	sed 's,@NAME@,stat,g' < $(sdir)/alias.c.in > $@

clean-here: clean.stat
clean.stat:
	rm -f stat.c
Make
