name=clone

moddeps="zsh/clone"

objects="clone.o"

:<<\Make
clone.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

clone.c: alias.c.in
	sed 's,@NAME@,clone,g' < $(sdir)/alias.c.in > $@

clean-here: clean.clone
clean.clone:
	rm -f clone.c
Make
