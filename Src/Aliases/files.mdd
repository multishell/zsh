name=files

moddeps="zsh/files"

objects="files.o"

:<<\Make
files.mdd: alias.mdd.in
	rm -f $@
	cd $(sdir) && ./.preconfig

files.c: alias.c.in
	sed 's,@NAME@,files,g' < $(sdir)/alias.c.in > $@

clean-here: clean.files
clean.files:
	rm -f files.c
Make
